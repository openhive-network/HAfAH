---
  name: Common test values
  description: Common values for tests

  variables:
    service:
      proto: https
      server: "{tavern.env_vars.HIVEMIND_ADDRESS}"
      port: "{tavern.env_vars.HIVEMIND_PORT}"
