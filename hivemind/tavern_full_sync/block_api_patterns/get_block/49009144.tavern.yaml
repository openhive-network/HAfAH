---
  test_name: Hivemind block_api.get_block

  marks:
    - fullsynctest # slow call - 2.016s

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_block
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "block_api.get_block"
          params: {'block_num': 49009144}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "49009144"
            directory: "block_api_patterns/get_block"
