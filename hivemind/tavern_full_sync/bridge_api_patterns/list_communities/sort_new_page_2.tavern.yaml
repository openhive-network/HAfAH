---
  test_name: Hivemind bridge_api.list_communities sort_new page 2

  marks:
    - fullsynctest # is this return

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_communities sort_new page 2
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_communities"
          params: {"sort":"new","last": "hive-181829"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "sort_new_page_2"
            directory: "bridge_api_patterns/list_communities"
