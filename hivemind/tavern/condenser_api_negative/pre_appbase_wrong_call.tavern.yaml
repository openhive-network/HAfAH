---
  test_name: Hivemind condenser_api pre_appbase test with nonexisting method

  marks:
    - patterntest
    - negative

  includes:
    - !include ../common.yaml

  stages:
    - name: condenser_api pre_appbase test with nonexisting method
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "call"
          params: ["condenser_api", "find_comments", ["gtg","witness-gtg"]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "pre_appbase_wrong_call"
            directory: "condenser_api_negative"
            error_response: true