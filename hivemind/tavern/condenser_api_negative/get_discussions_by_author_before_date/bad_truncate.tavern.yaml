---
  test_name: Hivemind condenser_api.get_discussions_by_author_before_date

  marks:
    - patterntest # original didn't handle truncation in this call
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_author
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_author_before_date"
          params: {"author":"steemit","limit":1,"truncate_body":"five"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "bad_truncate"
            directory: "condenser_api_negative/get_discussions_by_author_before_date"
            error_response: true