---
  test_name: Hivemind hive_api smoke test

  marks:
    - smoketest

  includes:
    - !include common.yaml

  stages:
    - name: db_head_state
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "hive.db_head_state"
          params: {}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:validate_response
