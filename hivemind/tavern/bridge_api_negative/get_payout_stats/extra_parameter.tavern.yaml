---
  test_name: Hivemind bridge_api.get_payout_stats extra parameter test

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_payout_stats extra parameter
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_payout_stats"
          params: {"limit": 100, "extra_parameter": "valid"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "extra_parameter"
            directory: "bridge_api_negative/get_payout_stats"
            error_response: true
