---
  test_name: Hivemind bridge_api.list_community_roles non_existing_last

  marks:
    - patterntest # original did not validate community
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: list_community_roles non_existing_last
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_community_roles"
          params: {"community":"hive-117600", "last":"nonexisting"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "non_existing_last"
            directory: "bridge_api_negative/list_community_roles"
            error_response: true
