---
  test_name: Hivemind bridge_api.account_notifications pre_appbase patterns test

  marks:
    - patterntest # `call` requires condenser_api

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.account_notifications pre_appbase patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "call"
          params: ["bridge_api", "account_notifications", ["steemit",15, 20]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "pre_appbase"
            directory: "bridge_api_negative/account_notifications"
            error_response: true