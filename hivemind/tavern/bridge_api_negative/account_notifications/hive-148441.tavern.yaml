---
  test_name: Hivemind bridge_api.account_notifications hive community

  marks:
    - patterntest # communities not present in 5mln blocks

  includes:
    - !include ../../common.yaml

  stages:
    - name: account_notifications hive community
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.account_notifications"
          params: {"account":"hive-148441","limit":50}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "hive-148441"
            directory: "bridge_api_negative/account_notifications"
            error_response: true

