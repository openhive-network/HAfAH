---
  test_name: Hivemind bridge.get_account_posts invalid sort feed start author

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts invalid sort feed start author
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"feed", "account":"blocktrades", "start_author": "invalid_account"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "invalid_start_author"
            directory: "bridge_api_negative/get_account_posts/feed"
            error_response: true