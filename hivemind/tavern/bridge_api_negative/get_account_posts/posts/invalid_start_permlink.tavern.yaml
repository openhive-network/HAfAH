---
  test_name: Hivemind bridge.get_account_posts sort posts invalid start permlink

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort posts invalid start permlink
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"posts", "account":"blocktrades", "start_author": "blocktrades","start_permlink":98}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "invalid_start_permlink"
            directory: "bridge_api_negative/get_account_posts/posts"
            error_response: true