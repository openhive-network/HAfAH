---
  test_name: Hivemind bridge_api.get_relationship_between_accounts not specified account 2 # 8080

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_relationship_between_accounts not specified account 2
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_relationship_between_accounts"
          params: {"account1": "tinfoilfedora","account2": "",}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "not_specified_account2"
            directory: "bridge_api_negative/get_relationship_between_accounts"
            error_response: true
