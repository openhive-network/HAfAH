---
  test_name: bridge_api.list_subscribers undefined operator # not documented

  marks:
    - patterntest # used to have a bug related to invalid call when nonstring was passed
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_subscribers undefined operator
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_subscribers"
          params: [789]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "undefined_operator"
            directory: "bridge_api_negative/list_subscribers"
            error_response: true