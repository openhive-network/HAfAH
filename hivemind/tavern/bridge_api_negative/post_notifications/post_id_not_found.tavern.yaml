---
  test_name: bridge_api.post_notifications post id not found
    # author and permlink must be both to find post id

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.post_notifications post id not found
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.post_notifications"
          params: ["steemit","", ""]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "post_id_not_found"
            directory: "bridge_api_negative/post_notifications"
            error_response: true
