---
  test_name: bridge_api.list_pop_communities invalid literal steemit # 8080
  # not documented on https://developers.hive.io/apidefinitions/#apidefinitions-bridge
  # no nonempty result possible for 5mln set

  marks:
    - patterntest # change to proper error, server error is barely acceptable here
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_pop_communities invalid literal steemit
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_pop_communities"
          params: ["steemit"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "invalid_literal_steemit"
            directory: "bridge_api_negative/list_pop_communities"
            error_response: true