---
  test_name: bridge_api.list_pop_communities too many positional arguments # 8080

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_pop_communities too many positional arguments
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_pop_communities"
          params: ["", "1"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "too_many_positional_arguments"
            directory: "bridge_api_negative/list_pop_communities"
            error_response: true
