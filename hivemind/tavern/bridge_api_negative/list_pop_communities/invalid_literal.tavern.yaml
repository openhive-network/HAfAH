---
  test_name: bridge_api.list_pop_communities invalid literal # 8080 023

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_pop_communities invalid literal
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_pop_communities"
          params: ["abc"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "invalid_literal"
            directory: "bridge_api_negative/list_pop_communities"
            error_response: true
