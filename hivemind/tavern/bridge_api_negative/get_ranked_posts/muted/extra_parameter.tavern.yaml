---
  test_name: Hivemind bridge_api.get_ranked_posts invalid extra parameter

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts invalid extra parameter
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"muted", "tag":"", "start_author":"gtg", "permlink": "extra parameter"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "extra_parameter"
            directory: "bridge_api_negative/get_ranked_posts/muted"
            error_response: true
