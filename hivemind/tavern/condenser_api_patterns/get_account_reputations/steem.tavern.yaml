---
  test_name: Hivemind condenser_api.get_account_reputations patterns test #8080

  marks:
    - patterntest # fat node had slightly different implementation, did not accept arguments by name (.fatnode pattern made with positional call)

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_account_reputations
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_account_reputations"
          params: {"account_lower_bound": "steem", "limit":1}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "steem"
            directory: "condenser_api_patterns/get_account_reputations"
