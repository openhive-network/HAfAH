---
  test_name: Hivemind condenser_api.get_discussions_by_promoted patterns test observer alice

  marks:
    - patterntest # alice follows muted of ignoreall who ignores fyrstikken (mock data); see also no_params

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.get_discussions_by_promoted observer alice
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_promoted"
          params: {"observer":"alice"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "indirect_muted"
            directory: "condenser_api_patterns/get_discussions_by_promoted"
