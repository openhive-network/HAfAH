---
  test_name: Hivemind condenser_api.get_discussions_by_promoted patterns test observer alice with paging mechanism

  marks:
    - patterntest # alice follows muted of ignoreall who ignores fyrstikken (mock data); see also indirect_muted

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.get_discussions_by_promoted observer alice with paging
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_promoted"
          params: {"start_author": "horizonkinetics", "start_permlink": "technical-problems-of-a-fixed-money-supply", 
                   "limit": 3, "observer": "alice"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "indirect_muted_paging"
            directory: "condenser_api_patterns/get_discussions_by_promoted"
