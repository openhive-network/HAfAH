---
  test_name: Hivemind condenser_api.get_discussions_by_trending patterns test

  marks:
    - patterntest # post @fabio/tag-vorschlag-stelldichvor not present in original (despite proper trend value) because it was edited and lost
                  # 'introduceyourself' tag in last edit (but new hivemind does not make such edits - it needs to be fixed at some point)

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_trending
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_trending"
          params: {"tag":"introduceyourself","limit":10}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_introduceyourself"
            directory: "condenser_api_patterns/get_discussions_by_trending"