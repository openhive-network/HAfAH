---
  test_name: Hivemind condenser_api.get_content invalid observer

  marks:
    - patterntest # does not validate observer in any way at the moment
   # - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_content invalid observer
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_content"
          params: {"author": "gtg", "permlink": "hello-world","observer": "notexisttest"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "invalid_observer"
            directory: "condenser_api_patterns/get_content"
           # error_response: true