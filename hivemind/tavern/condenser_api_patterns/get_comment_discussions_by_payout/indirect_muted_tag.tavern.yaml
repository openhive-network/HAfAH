---
  test_name: Hivemind condenser_api.get_comment_discussions_by_payout patterns test observer alice with tag

  marks:
    - patterntest # alice follows mute list of ignoreall who ignores roland.haynes (mock data); see also tag_steemit

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_comment_discussions_by_payout observer alice with tag
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_comment_discussions_by_payout"
          params: {"tag":"steemit","observer":"alice"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "indirect_muted_tag"
            directory: "condenser_api_patterns/get_comment_discussions_by_payout"
