---
  test_name: Hivemind condenser_api.get_comment_discussions_by_payout patterns test tag steemit

  marks:
    - patterntest # test to compare with observers alice and ignoreall

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_comment_discussions_by_payout ignoreall with tag and paging mechanism
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_comment_discussions_by_payout"
          params: {"tag":"steemit"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "steemit"
            directory: "condenser_api_patterns/get_comment_discussions_by_payout"
