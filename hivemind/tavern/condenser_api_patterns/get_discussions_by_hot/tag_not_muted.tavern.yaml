---
  test_name: Hivemind condenser_api.get_discussions_by_hot patterns test observer tag steemit

  marks:
    - patterntest # test to compare with observers alice and ignoreall tag steemit

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_hot tag steemit
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_hot"
          params: {"tag":"steemit"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_not_muted"
            directory: "condenser_api_patterns/get_discussions_by_hot"
