---
  test_name: Hivemind condenser_api.get_discussions_by_hot patterns test observer alice with paging

  marks:
    - patterntest # alice follows muted of ignoreall who ignores gavvet (mock data);  see also paging

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_hot observer alice with paging
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_hot"
          params: {"observer":"alice","limit":3,
                   "start_author":"dollarvigilante","start_permlink":"another-billionaire-warns-of-catastrophic-depths-not-seen-in-5-000-years-and-emphasizes-gold"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "indirect_muted_paging"
            directory: "condenser_api_patterns/get_discussions_by_hot"
