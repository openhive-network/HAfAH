---
  test_name: Hivemind condenser_api.get_discussions_by_hot patterns test tag and paging

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_hot tag and paging
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_hot"
          params: {"tag":"steemit", "limit":3,
                   "start_author":"steemsmart","start_permlink":"who-needs-steemit-etiquette-steem-smart-podcast-ep-11a"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_paging_no_observer"
            directory: "condenser_api_patterns/get_discussions_by_hot"
