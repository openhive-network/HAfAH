---
  test_name: Hivemind condenser_api.get_reblogged_by pre_appbase patterns test

  marks:
    - patterntest # bug in old hivemind

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.get_reblogged_by pre_appbase patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "call"
          params: ["condenser_api", "get_reblogged_by", ["skypilot", "melting-rock"]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "pre_appbase"
            directory: "condenser_api_patterns/get_reblogged_by"
