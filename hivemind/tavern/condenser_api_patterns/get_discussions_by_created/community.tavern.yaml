---
  test_name: Hivemind condenser_api.get_discussions_by_created community patterns test

  marks:
    - patterntest # communities not implemented under 5 mln blocks

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.get_discussions_by_created community patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_created"
          params: {"tag":"hive-173493", "limit":5}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community"
            directory: "condenser_api_patterns/get_discussions_by_created"
