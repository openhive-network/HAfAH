---
  test_name: Hivemind condenser_api.get_discussions_by_created patterns test

  marks:
    - patterntest # if user is on the list it can filter out itself :)

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_created funny_follow_muted_self
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.get_discussions_by_created"
          params: {"start_author":"liz-wolfe", "start_permlink":"hi-steemit-im-liz-20-y-o","limit":100, "observer":"funny"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "funny_follow_muted_self"
            directory: "condenser_api_patterns/get_discussions_by_created"