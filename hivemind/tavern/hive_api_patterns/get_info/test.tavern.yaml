---
  test_name: Hivemind hive.get_info patterns test test

  marks:
    - patterntest # incorrect version and git revision version

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_info
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "hive.get_info"
          params: {}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "test"
            directory: "hive_api_patterns/get_info"

