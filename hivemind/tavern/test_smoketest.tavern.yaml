---
  test_name: Hivemind smoke test

  includes:
    - !include common.yaml

  stages:
    - name: Validate hivemind is running
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port:d}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: ""
          params: {}
      response:
        status_code: 200

