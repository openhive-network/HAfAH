---
  test_name: bridge_api.list_communities empty_last

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.list_communities community_empty_last
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_communities"
          params: {"last":""}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "empty_last"
            directory: "bridge_api_patterns/list_communities"
