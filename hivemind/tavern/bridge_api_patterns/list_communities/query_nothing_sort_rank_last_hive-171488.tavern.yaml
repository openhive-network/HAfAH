---
  test_name: Hivemind bridge_api.list_communities patterns test query Nothing sort rank last hive-171488
      # not documented on https://developers.hive.io/apidefinitions/#apidefinitions-bridge
  marks:
    - patterntest
    # Communities not implemented under 5 mln blocks, but some were created by mock mechanism, therefore they are in result.

  includes:
    - !include ../../common.yaml

  stages:
    - name: list_communities query Nothing sort rank last hive-171488
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_communities"
          params: {"last":"hive-171488", "limit":100, "query":"Nothing", "sort": "rank"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "query_nothing_sort_rank_last_hive-171488"
            directory: "bridge_api_patterns/list_communities"
