---
  test_name: Hivemind bridge_api.get_trending_topics patterns with gtg observer test

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_trending_topics with gtg observer
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_trending_topics"
          params: {"limit": 25, "observer":"gtg"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "with_gtg_observer"
            directory: "bridge_api_patterns/get_trending_topics"
