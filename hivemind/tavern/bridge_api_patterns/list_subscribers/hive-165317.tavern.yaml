---
  test_name: Hivemind bridge_api.list_subscribers hive-165317 patterns test
      # not documented on https://developers.hive.io/apidefinitions/#apidefinitions-bridge
  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: list_subscribers hive-165317
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_subscribers"
          params: {"community":"hive-165317"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "hive-165317"
            directory: "bridge_api_patterns/list_subscribers"
