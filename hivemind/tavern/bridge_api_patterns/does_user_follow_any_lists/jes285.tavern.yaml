---
  test_name: Hived bridge_api does_user_follow_any_lists jes285

  marks:
    - patterntest # tested on anyx.io

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api does_user_follow_any_lists jes285
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.does_user_follow_any_lists"
          params: {"observer": "jes285"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "jes285"
            directory: "bridge_api_patterns/get_follow_list"
