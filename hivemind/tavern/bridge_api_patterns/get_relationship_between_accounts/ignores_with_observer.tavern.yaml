---
  test_name: Hivemind bridge_api.get_relationship_between_accounts ignores with observer patterns test

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_relationship_between_accounts ignores with observer
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_relationship_between_accounts"
          params: {"account1": "tuck-fheman", "account2": "tonyson", "observer":"gtg"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "ignores_with_observer"
            directory: "bridge_api_patterns/get_relationship_between_accounts"
