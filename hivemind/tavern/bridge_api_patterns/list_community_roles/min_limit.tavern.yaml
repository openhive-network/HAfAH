---
  test_name: Hivemind bridge_api.list_community_roles patterns test min limit

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: list_community_roles min limit
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_community_roles"
          params: {"community":"hive-117600", "limit":1}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "min_limit"
            directory: "bridge_api_patterns/list_community_roles"
