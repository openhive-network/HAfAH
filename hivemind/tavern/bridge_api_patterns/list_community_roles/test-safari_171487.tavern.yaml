---
  test_name: Hivemind bridge_api.list_community_roles patterns test last test-safari hive-171487

  marks:
    - patterntest # mock-data - test-safari is in different communities

  includes:
    - !include ../../common.yaml

  stages:
    - name: list_community_roles test-safari hive-171487
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_community_roles"
          params: {"community":"hive-171487", "last":"test-safari", "limit":100}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "test-safari_171487"
            directory: "bridge_api_patterns/list_community_roles"
