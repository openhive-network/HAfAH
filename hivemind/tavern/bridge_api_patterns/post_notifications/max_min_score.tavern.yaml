---
  test_name: Hivemind bridge.post_notifications patterns test max min_score
  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge.post_notifications max min_score
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.post_notifications"
          params: {"author":"pharesim",
                   "permlink": "witness-post",
                   "min_score":100}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "max_min_score"
            directory: "bridge_api_patterns/post_notifications"