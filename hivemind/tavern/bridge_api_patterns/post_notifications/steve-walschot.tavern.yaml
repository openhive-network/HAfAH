---
  test_name: Hivemind bridge.post_notifications patterns test steve-walschot

  marks:
    - patterntest # test to check paging (first 100 results)

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge.post_notifications steve-walschot
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.post_notifications"
          params: {"author":"steve-walschot",
                   "permlink": "steeming-around-the-clock-bye-bye-regular-job"
                   }
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "steve-walschot"
            directory: "bridge_api_patterns/post_notifications"