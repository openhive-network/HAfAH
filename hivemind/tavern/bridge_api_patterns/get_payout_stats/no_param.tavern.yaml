---
  test_name: Hivemind bridge_api.get_payout_stats patterns test no parameters

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_payout_stats no parameters
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_payout_stats"
          params: {}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "no_param"
            directory: "bridge_api_patterns/get_payout_stats"
