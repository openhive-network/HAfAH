---
  test_name: Hivemind condenser_api.get_discussions_by_created patterns test

  marks:
    - patterntest # nkdk is correctly follow blacklisted "blacklisted by blacklisttester"

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_created followtest_follow_blacklist
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author":"nkdk", "permlink":"thermosolar-hive-healthy-bees-healthy-honey", "observer":"followtest"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "followtest_follow_blacklist"
            directory: "bridge_api_patterns/get_discussion"