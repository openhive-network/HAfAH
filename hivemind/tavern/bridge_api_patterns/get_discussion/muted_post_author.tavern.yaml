---
  test_name: Hivemind bridge_api.get_discussion steemit muted in community patterns test

  marks:
    - patterntest # steemit is muted in this commmunity - mock_data (hive-198723)

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.get_discussion steemit as muted patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author":"steemit", "permlink":"muted-in-this-community"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "muted_post_author"
            directory: "bridge_api_patterns/get_discussion"
