---
  test_name: Hivemind condenser_api.get_discussions_by_created patterns test

  marks:
    - patterntest # cryptos is correctly on "my blacklist"

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_created followtest_blacklisted
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author":"cryptos", "permlink":"getting-started-mining-steem-on-windows", "observer":"followtest"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "followtest_blacklisted"
            directory: "bridge_api_patterns/get_discussion"