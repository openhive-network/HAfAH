---
  test_name: Hivemind bridge_api.get_discussion alice muted of community patterns test

  marks:
    - patterntest # alice is muted before answering parent post in this community - mock_data (hive-171487)
                  # unmuted all comments

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.get_discussion alice as member patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author":"test-safari", "permlink":"please-comment-hive1"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "unmute_comment_muted"
            directory: "bridge_api_patterns/get_discussion"
