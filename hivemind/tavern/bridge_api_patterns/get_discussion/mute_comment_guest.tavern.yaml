---
  test_name: Hivemind bridge_api.get_discussion alice guest of community patterns test

  marks:
    - patterntest # alice is guest before answering parent post in this community - mock_data (hive-157439)
                  # muted all comments (not unmuted later)

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.get_discussion alice as guest patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author":"test-safari", "permlink":"please-comment-hive2-02"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "mute_comment_guest"
            directory: "bridge_api_patterns/get_discussion"
