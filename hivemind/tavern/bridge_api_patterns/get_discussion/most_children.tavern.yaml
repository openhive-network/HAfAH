---
  test_name: Hivemind bridge_api.get_discussion post with the most children patterns test

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.get_discussion post with the most children patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_discussion"
          params: {"author": "rolypoli20", "permlink": "poli-s-grand-kpop-album-giveaway-promo"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "most_children"
            directory: "bridge_api_patterns/get_discussion"
