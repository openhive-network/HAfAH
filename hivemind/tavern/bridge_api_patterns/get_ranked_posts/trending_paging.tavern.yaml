---
  test_name: Hivemind bridge_api.get_ranked_posts trading paging patterns test

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts trading paging patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"trending","tag":"", "start_author":"curie","start_permlink":"the-daily-curie-14th-sept-15th-sept-2016", "limit":2}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "trending_paging"
            directory: "bridge_api_patterns/get_ranked_posts"
