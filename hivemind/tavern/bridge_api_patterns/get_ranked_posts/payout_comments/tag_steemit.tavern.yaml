---
  test_name: Hivemind bridge_api.get_ranked_posts payout_comments tag steemit patterns test

  marks:
    - patterntest # all tags for payout_comments returns empty data, it is correct?

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts promoted tag eth patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"payout_comments","tag":"steemit"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_steemit"
            directory: "bridge_api_patterns/get_ranked_posts/payout_comments"
