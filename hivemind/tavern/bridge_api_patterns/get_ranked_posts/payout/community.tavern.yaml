---
  test_name: Hivemind bridge_api.get_ranked_posts payout community patterns test

  marks:
    - patterntest # community exists but there is no posts related

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts payout community patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"payout", "tag":"hive-117600", "limit":5}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community"
            directory: "bridge_api_patterns/get_ranked_posts/payout"
