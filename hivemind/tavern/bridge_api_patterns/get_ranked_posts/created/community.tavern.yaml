---
  test_name: Hivemind bridge_api.get_ranked_posts created community patterns test

  marks:
    - patterntest # communities not implemented under 5 mln blocks

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts created community patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"created","tag":"hive-173493", "limit":5}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community"
            directory: "bridge_api_patterns/get_ranked_posts/created"
