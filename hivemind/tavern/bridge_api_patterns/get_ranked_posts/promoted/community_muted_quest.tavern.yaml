---
  test_name: Hivemind bridge_api.get_ranked_posts alice guest of community patterns test

  marks:
    - patterntest # alice is guest before answering parent post in this community - mock_data (hive-157439)
                  # muted all comments

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts alice as guest patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"start_author":"test-safari", "start_permlink":"please-comment-hive2-02", "sort": "promoted", "tag": "hive-157439"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community_muted_quest"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"
