---
  test_name: Hivemind bridge_api.get_ranked_posts promoted maximum limit patterns test

  marks:
    - patterntest # incomparable with original as now not only replies but top posts are in results as well (pattern accepted as-is, too big to create manually)
                  # blacklists matched with mock data

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts promoted maximum limit patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"promoted","tag":"","observer":"alice", "limit": 100}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "max_limit"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"
