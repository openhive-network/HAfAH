---
  test_name: Hivemind bridge_api.get_ranked_posts alice muted of community patterns test

  marks:
    - patterntest # alice is muted before answering parent post in this community - mock_data (hive-171487)
                  # unmuted all comments

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts alice as member patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"start_author":"test-safari", "start_permlink":"please-comment-hive1", "sort": "promoted", "tag": "hive-171487"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community_unmuted_muted"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"
