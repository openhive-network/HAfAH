---
  test_name: Hivemind bridge_api.get_ranked_posts alice member of community patterns test

  marks:
    - patterntest # alice is member before answering parent post in this community - mock_data (hive-198723)
                  # muted all comments

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts alice as member patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"start_author":"test-safari", "start_permlink":"please-comment-hive3-02", "sort": "promoted", "tag": "hive-198723"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community_muted_member"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"
