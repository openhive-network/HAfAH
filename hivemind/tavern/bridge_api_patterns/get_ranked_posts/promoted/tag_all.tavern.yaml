---
  test_name: Hivemind bridge_api.get_ranked_posts promoted tag all patterns test

  marks:
    - patterntest # change in behaviour compared to original, not only replies are in results, but top posts as well (initial pattern created with get_post calls)
                  # differences in blacklists are caused by lack of global/default blacklist - observer always needed

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts promoted tag all patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"promoted", tag: "all"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_all"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"
