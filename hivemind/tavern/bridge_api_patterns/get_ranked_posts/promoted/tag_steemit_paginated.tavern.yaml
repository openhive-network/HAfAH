---
  test_name: Hivemind bridge_api.get_ranked_posts promoted tag patterns test

  marks:
    - patterntest # same pattern as tag_steemit but starting below selected post

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts promoted tag patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort":"promoted","tag":"steemit", "start_author":"thecryptofiend", "start_permlink": "things-i-wish-i-d-known-when-i-was-18", "limit":10}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "tag_steemit_paginated"
            directory: "bridge_api_patterns/get_ranked_posts/promoted"