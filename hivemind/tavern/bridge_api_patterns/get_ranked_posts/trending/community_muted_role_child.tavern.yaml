---
  test_name: Hivemind bridge_api.get_ranked_posts child post author steemit muted in community patterns test

  marks:
    - patterntest # child post author - steemit - muted in this commmunity - mock_data (hive-198723)

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_ranked_posts child post author steemit muted patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_ranked_posts"
          params: {"sort": "trending", "tag": "hive-198723"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community_muted_role_child"
            directory: "bridge_api_patterns/get_ranked_posts/trending"
