---
  test_name: Hivemind bridge_api.get_account_posts sort blog observer patterns test

  marks:
    - patterntest # observer ignored - ignoreall mutes gtg

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort blog observer
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"blog", "account":"gtg", "observer": "ignoreall"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "observer"
            directory: "bridge_api_patterns/get_account_posts/blog"