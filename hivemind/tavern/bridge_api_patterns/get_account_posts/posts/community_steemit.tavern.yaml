---
  test_name: Hivemind bridge_api.get_account_posts steemit muted of community patterns test

  marks:
    - patterntest # steemit muted in community (mock_data)

  includes:
    - !include ../../../common.yaml

  stages:
    - name: bridge_api.get_account_posts steemit patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort": "posts", "account": "steemit"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "community_steemit"
            directory: "bridge_api_patterns/get_account_posts/posts"