---
  test_name: Hivemind bridge.get_account_posts sort by replies paginated test

  marks:
    - patterntest

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort by replies paginated
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"account": "blocktrades", "start_author": "piedpiper", "start_permlink": "re-blocktrades-witness-report-for-blocktrades-for-last-week-of-august-20160915t161707257z", "sort": "replies", "limit": 3}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "paginated"
            directory: "bridge_api_patterns/get_account_posts/replies"