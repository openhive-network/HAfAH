---
  test_name: Hivemind bridge_api.get_account_posts sort feed patterns test

  marks:
    - patterntest # see also paginated.tavern.yaml; role of observer is not yet clear (ignored for the time being)

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort feed
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"feed", "account":"always1success", "start_author":"knozaki2015", "start_permlink":"the-strangest-building-in-berlin", "limit":100, "observer":"alice"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "observer_alice"
            directory: "bridge_api_patterns/get_account_posts/feed"

