---
  test_name: Hivemind bridge.get_account_posts test # community test

  marks:
    - patterntest #todo find or create user with community

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"feed","account":"test-follower", "start_author":"test-creator", "start_permlink":"first-post"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "test-follower_page_2"
            directory: "bridge_api_patterns/get_account_posts/feed"