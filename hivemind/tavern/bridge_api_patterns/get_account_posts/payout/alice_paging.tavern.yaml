---
  test_name: Hivemind bridge_api.get_account_posts sort payout alice with paging test

  marks:
    - patterntest # mock_data - second and -02 posts muted
    # permlinks 'My-second-post-hive3'
    #           'Second-after-muted-post-hive2'
    #           'Second-after-muted-post-hive3'
    #           'after-muted-comment-to-please-comment-hive2-02'
    # are muted but have false in 'gray'
    # flag 'gray' as true in 'Second-after-muted-post-hive1' and 'after-muted-comment-to-please-comment-hive1-02'
    # flag works properly in community where alice had first role set as muted

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort payout alice with paging
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"payout", "account":"alice", "start_author": "alice", "start_permlink": "after-muted-comment-to-please-comment-hive3-02"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "alice_paging"
            directory: "bridge_api_patterns/get_account_posts/payout"