---
  test_name: Hivemind bridge_api.get_account_posts sort payout steemit test

  marks:
    - patterntest # mock_data - steemit muted in community 'hive-198723'

  includes:
    - !include ../../../common.yaml

  stages:
    - name: get_account_posts sort payout steemit
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_account_posts"
          params: {"sort":"payout", "account":"steemit"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "steemit"
            directory: "bridge_api_patterns/get_account_posts/payout"