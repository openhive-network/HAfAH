---
  test_name: Hivemind bridge_api.list_all_subscriptions patterns test gtg

  marks:
    - patterntest
    # SELECT name from hive_accounts where id IN (SELECT account_id from hive_subscriptions);
  includes:
    - !include ../../common.yaml

  stages:
    - name: list_all_subscriptions gtg
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.list_all_subscriptions"
          params: {"account":"gtg"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "gtg"
            directory: "bridge_api_patterns/list_all_subscriptions"
