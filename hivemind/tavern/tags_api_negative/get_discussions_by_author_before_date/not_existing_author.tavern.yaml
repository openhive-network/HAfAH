---
  test_name: Hivemind tags_api.get_discussions_by_author_before_date patterns test not existing author empty result

  marks:
    - patterntest # original didn't validate author
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_author_before_date
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "tags_api.get_discussions_by_author_before_date"
          params: ["kr3", "", "", 1]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "not_existing_author"
            directory: "tags_api_negative/get_discussions_by_author_before_date"
            error_response: true