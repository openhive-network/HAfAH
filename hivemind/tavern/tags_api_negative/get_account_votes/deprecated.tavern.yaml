---
  test_name: Hivemind deprecated tags_api.get_account_votes 

  marks:
    - patterntest
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_account_votes deprecated
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "tags_api.get_account_votes"
          params: {"account": "blocktrades"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "deprecated"
            directory: "tags_api_negative/get_account_votes"
            error_response: true