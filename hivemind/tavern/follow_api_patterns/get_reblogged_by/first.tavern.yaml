---
  test_name: Hivemind follow_api.get_reblogged_by patterns test

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_reblogged_by
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "follow_api.get_reblogged_by"
          params: {"author":"iamwne","permlink":"conceptualizing-a-scifi-land-speeder"}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "first"
            directory: "follow_api_patterns/get_reblogged_by"
