---
  test_name: Hivemind follow_api.get_blog patterns test test-116-05

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_blog
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "follow_api.get_blog"
          params: {"account":"test-116-05","start_entry_id":0,"limit":10}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "005"
            directory: "mock_tests/reblog_delete"

