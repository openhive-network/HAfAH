---
  test_name: Hivemind bridge_api.get_relationship_between_accounts

  marks:
    - patterntest # relies on mock data and extra unofficial argument

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_relationship_between_accounts
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "bridge.get_relationship_between_accounts"
          params: {"account1": "flw2", "account2": "flw1", "debug":true}
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "21"
            directory: "mock_tests/get_relationship_between_accounts"

