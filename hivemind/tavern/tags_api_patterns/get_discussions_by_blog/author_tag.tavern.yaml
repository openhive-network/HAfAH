---
  test_name: Hivemind tags_api.get_discussions_by_blog patterns test author tag

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: get_discussions_by_blog
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "tags_api.get_discussions_by_blog"
          params: ["dragonho", "life"] 
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "author_tag"
            directory: "tags_api_patterns/get_discussions_by_blog"