---
  test_name: Hivemind database_api.find_comments pre_appbase patterns test

  marks:
    - patterntest # `call` requires condenser_api
    - negative

  includes:
    - !include ../../common.yaml

  stages:
    - name: database_api.find_comments pre_appbase patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          $ext:
            function: validate_response:get_headers
        json:
          jsonrpc: "2.0"
          id: 1
          method: "call"
          params: ["database_api", "find_comments", ["gtg","hello-world"]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "pre_appbase"
            directory: "database_api_negative/find_comments"
            error_response: true