---
  test_name: Hivemind follow_api.get_blog pre_appbase patterns test

  marks:
    - patterntest # `call` requires condenser_api

  includes:
    - !include ../../common.yaml

  stages:
    - name: follow_api.get_blog pre_appbase patterns test
      request:
        url: "{service.proto:s}://{service.server:s}:{service.port}/"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "call"
          params: ["follow_api", "get_blog", ["gtg","hello-world"]]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "pre_appbase"
            directory: "follow_api_negative/get_blog"
            error_response: true