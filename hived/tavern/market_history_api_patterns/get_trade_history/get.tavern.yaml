---
  test_name: Hived market_history_api.get_trade_history

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: market_history_api.get_trade_history
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "market_history_api.get_trade_history"
          params: {
            "bucket_seconds": 1000,
            "start": "2016-08-11T00:00:00",
            "end": "2017-01-11T00:00:00"
          }
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:has_valid_response
          extra_kwargs:
            method: "get"
            directory: "market_history_api_patterns/get_trade_history"
