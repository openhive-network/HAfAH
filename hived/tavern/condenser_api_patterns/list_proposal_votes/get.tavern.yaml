---
  test_name: Hived condenser_api.list_proposal_votes

  marks:
    - patterntest

  includes:
    - !include ../../common.yaml

  stages:
    - name: condenser_api.list_proposal_votes
      request:
        url: "{service.url:s}"
        method: POST
        headers:
          content-type: application/json
        json:
          jsonrpc: "2.0"
          id: 1
          method: "condenser_api.list_proposal_votes"
          params: [['gtg'], 10, "by_voter_proposal", "ascending", "expired"]
      response:
        status_code: 200
        verify_response_with:
          function: validate_response:compare_response_with_pattern
          extra_kwargs:
            method: "get"
            directory: "condenser_api_patterns/list_proposal_votes"
            ignore_tags: ['id', 'proposal', 'voter']