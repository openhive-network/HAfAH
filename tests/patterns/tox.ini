[tox]
envlist = py38 py36
skipsdist = True

[testenv]
setenv =
    AH_ENDPOINT = {env:AH_ENDPOINT:}
deps =
    pytest==6.2.5
    pytest-timeout==2.0.1
    pytest-repeat==0.9.1
    requests==2.25.1
    tavern==1.19.0

commands =
    # pytest tavern/account_history_api_patterns/get_ops_in_block/only_virtual_false.tavern.yaml
    # pytest tavern/account_history_api_patterns/get_ops_in_block/block_4502370.tavern.yaml
    # pytest -s --junitxml="./report.xml" {posargs}
    pytest
